const [lading, setLoading] = useState(false);

useEffect(
    setLoading(true);
    console.log("Request is loading");
        axios.rq().finally(() => {setLoading(false)})
, [])


return {
    isLoading ? <spinner></spinnr> : <data>
}